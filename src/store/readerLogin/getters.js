export function getReaderId(state) {
  return state.reader.id;
}

export function getReaderName(state) {
  return state.reader.name;
}

export function getPostCommentId(state) {
  return state.postCommentId;
}
